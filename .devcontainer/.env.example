# EcoFlow Cloud Auto-Configuration
# Copy this file to .env and fill in your credentials

# Required: Your EcoFlow Public API credentials
# Get these from: https://developer.ecoflow.com/
ECOFLOW_ACCESS_KEY=your_access_key_here
ECOFLOW_SECRET_KEY=your_secret_key_here

# Required: Devices to configure (JSON array)
# Each device needs: sn (serial number), name (friendly name), type (device type)
# Common device types: Smart Home Panel, DELTA 2, DELTA PRO, RIVER 2, RIVER 2 PRO, etc.
# IMPORTANT: Wrap JSON in single quotes to prevent shell interpretation!
# Example with one device:
ECOFLOW_DEVICES='[{"sn": "YOUR_DEVICE_SN", "name": "Smart Home Panel", "type": "Smart Home Panel"}]'

# Example with multiple devices:
# ECOFLOW_DEVICES=[{"sn": "SN1", "name": "Delta Pro", "type": "DELTA_PRO"}, {"sn": "SN2", "name": "River 2", "type": "RIVER_2"}]

# Optional: API host (default: api-e.ecoflow.com for Europe)
# Use api-a.ecoflow.com for Americas/Asia
# ECOFLOW_API_HOST=api-e.ecoflow.com

# Optional: Group name (default: Home)
# ECOFLOW_GROUP=Home
